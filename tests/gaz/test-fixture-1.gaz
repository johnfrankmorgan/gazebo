load str; // FIXME: parse error if assignment follows load

f = fun (word, letter) {
  if str.contains?(word, letter) {
    return -32
  } else {
    return !true
  }
}

for i = 1; i < 20; i += 1
  println(i)

i = 1
while (i > 0) {
  i -= 1
  pass
}

list = [1, 2, 3, 4]

println(list.index(0))

unset i; // FIXME: parse error if assignment follows unset

t = 100
